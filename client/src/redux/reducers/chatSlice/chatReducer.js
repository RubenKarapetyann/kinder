

function chatReducer(state={ loading : false, list : [] },action){
    switch(action.type){
        case "":
            return state
        default:
            return state
    }
}

export default chatReducer