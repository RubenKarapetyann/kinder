
function profileReducer(state={ loading : false, profile : {} }, action){
    switch(action.type){
        case "":
            return state
        default:
            return state
    }
}

export default profileReducer